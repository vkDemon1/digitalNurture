git status
git branch GitWork
git checkout GitWork
echo "<text>Hello from the Branch</text>" > hello.xml
git add hello.xml
git status
git commit -m "Added hello.xml to GitWork branch"
git checkout master
echo "<text>Hello from the Master</text>" > hello.xml
git add hello.xml
git commit -m "Added hello.xml to master branch"
git log --oneline --graph --decorate --all
git diff master..GitWork
git difftool master..GitWork
git merge GitWork
cat hello.xml
git mergetool
git commit -m "Resolved merge conflict in hello.xml"
git status
echo "*.orig" >> .gitignore
git add .gitignore
git commit -m "Ignored merge backup files"
git branch -a
git branch -d GitWork
git log --oneline --graph --decorate
git push origin master
